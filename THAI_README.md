# ✅ สำเร็จแล้ว! APK พร้อมใช้งาน

## 🎉 ไฟล์ APK ถูกสร้างเรียบร้อยแล้ว

### 📍 ตำแหน่งไฟล์
```
C:\Programing\AI2.0\ttsVoice\TTSVoiceApp\android\app\build\outputs\apk\debug\app-debug.apk
```

### 📊 รายละเอียดไฟล์
- **ชื่อไฟล์:** app-debug.apk
- **ขนาด:** 107.7 MB (107,776,595 bytes)
- **เวอร์ชัน:** 1.0 (Debug Build)
- **วันที่สร้าง:** 3 กุมภาพันธ์ 2026 - 20:54:14

---

## 📱 ขั้นตอนการติดตั้งบน Android

### วิธีที่ 1: ติดตั้งผ่าน USB (แนะนำ)

1. **เปิด USB Debugging บน Android**
   - ไปที่ Settings → About Phone
   - แตะ "Build Number" 7 ครั้งเพื่อเปิด Developer Mode
   - กลับไปที่ Settings → Developer Options
   - เปิด "USB Debugging"

2. **เชื่อมต่อ Android เข้ากับคอมพิวเตอร์**
   - ใช้สาย USB เชื่อมต่อ
   - บนโทรศัพท์จะมีข้อความขออนุญาต USB Debugging → กด "อนุญาต"

3. **ติดตั้งผ่าน adb**
   ```powershell
   cd C:\Programing\AI2.0\ttsVoice\TTSVoiceApp\android
   adb install app\build\outputs\apk\debug\app-debug.apk
   ```

### วิธีที่ 2: คัดลอกไฟล์ไปยัง Android (ง่ายที่สุด)

1. **คัดลอกไฟล์ APK**
   - คัดลอก `app-debug.apk` ไปยัง Android ผ่าน:
     - USB Cable → คัดลอกเข้า Downloads folder
     - Google Drive / Dropbox
     - LINE / Email แนบไฟล์

2. **เปิดการติดตั้งจากแหล่งที่ไม่รู้จัก**
   - Settings → Security / Apps
   - เปิด "Install from Unknown Sources" หรือ "Allow from this source"

3. **ติดตั้ง**
   - เปิด File Manager บน Android
   - หาไฟล์ `app-debug.apk`
   - แตะเพื่อติดตั้ง
   - กด "ติดตั้ง" / "Install"

---

## 🚀 วิธีใช้งานหลังติดตั้ง

### 1. เปิดแอพครั้งแรก

1. เปิดแอพ "TTSVoiceApp" จาก App Drawer
2. แอพจะขออนุญาตต่างๆ → **อนุญาตทั้งหมด**
3. กด **"▶️ Start Service"** เพื่อเริ่มบริการ

### 2. ติดตั้ง Google Text-to-Speech (สำคัญ!)

**สำหรับภาษาไทย:**
1. เปิด Play Store
2. ค้นหา "Google Text-to-Speech"
3. กด Install / Update
4. ไปที่ Settings → Language & Input → Text-to-speech
5. ดาวน์โหลด Thai Language Pack ถ้ายังไม่มี

### 3. ตั้งค่าให้ทำงานต่อเนื่อง (แนะนำ)

**ปิด Battery Optimization:**
1. Settings → Apps → TTSVoiceApp → Battery
2. เลือก "Unrestricted" หรือ "Don't optimize"

**เปิด Auto-start (บางยี่ห้อ):**
1. Settings → Apps → TTSVoiceApp → Permissions
2. หา "Autostart" หรือ "Auto-start" → เปิด
3. (Xiaomi: Security → Permissions → Autostart)

---

## 🎮 วิธีทดสอบการทำงาน

### ทดสอบจากแอพ

1. **เปิดแอพ TTSVoiceApp**
2. **กด "Start Service"** (ถ้ายังไม่ได้กด)
3. **พิมพ์ข้อความ:**
   - ภาษาไทย: "สวัสดีครับ" หรือ "ทดสอบระบบเสียง"
   - ภาษาอังกฤษ: "Hello World"
4. **กด "🔊 Speak Text"**
5. **ควรได้ยินเสียงพูดออกมาจากลำโพง**

### หา IP Address ของ Android

**วิธีที่ 1: จากแอพ**
- ดูที่ส่วน "Service Status" → "Server: http://xxx.xxx.xxx.xxx:8765"

**วิธีที่ 2: จาก Settings**
- Settings → About → Status → IP Address
- หรือ Settings → Wi-Fi → แตะ Wi-Fi ที่เชื่อมต่ออยู่ → ดู IP

---

## 🌐 ทดสอบ REST API จากคอมพิวเตอร์

### ข้อกำหนดเบื้องต้น
- Android และ Computer อยู่ Wi-Fi เดียวกัน
- บริการบน Android ต้องรันอยู่ (เห็นสถานะเป็นสีเขียว)
- จด IP Address ของ Android (เช่น 192.168.1.100)

### ทดสอบด้วย Python

**แก้ไข test_client.py:**
```python
# เปิดไฟล์ test_client.py
# เปลี่ยน DEVICE_IP เป็น IP ของ Android คุณ
DEVICE_IP = "192.168.1.100"  # <-- เปลี่ยนตรงนี้
```

**รันทดสอบ:**
```powershell
# ทดสอบพูดภาษาไทย
python test_client.py speak "สวัสดีครับ"

# ทดสอบพูดภาษาอังกฤษ
python test_client.py speak "Hello World"

# เช็คสถานะ
python test_client.py status

# เช็ค Health
python test_client.py health
```

### ทดสอบด้วย curl (PowerShell)

```powershell
# เปลี่ยน 192.168.1.100 เป็น IP ของ Android คุณ

# พูดภาษาไทย
curl -X POST http://192.168.1.100:8765/speak `
  -H "Content-Type: application/json" `
  -d '{"text":"สวัสดีครับ"}'

# พูดภาษาอังกฤษ
curl -X POST http://192.168.1.100:8765/speak `
  -H "Content-Type: application/json" `
  -d '{"text":"Hello World"}'

# เช็คสถานะ
curl http://192.168.1.100:8765/status

# Health Check
curl http://192.168.1.100:8765/health
```

---

## 📚 API Reference

### Base URL
```
http://<android-ip>:8765
```

### Endpoint 1: พูดข้อความ
**POST /speak**

**Request Body:**
```json
{
  "text": "สวัสดีครับ"
}
```

**Response:**
```json
{
  "status": "queued",
  "text": "สวัสดีครับ",
  "queueSize": 1,
  "message": "Text added to speech queue"
}
```

### Endpoint 2: เช็คสถานะ
**GET /status**

**Response:**
```json
{
  "service": "running",
  "port": 8765,
  "ttsInitialized": true,
  "isSpeaking": false,
  "queueSize": 0,
  "lastStatus": "Running on port 8765"
}
```

### Endpoint 3: Health Check
**GET /health**

**Response:**
```json
{
  "status": "healthy"
}
```

---

## 🔧 แก้ปัญหาที่พบบ่อย

### ปัญหา: บริการไม่ทำงาน / หยุดเอง

**วิธีแก้:**
1. เช็คว่ากด Start Service แล้ว
2. ปิด Battery Optimization
3. ตั้ง Autostart (บางยี่ห้อ)
4. ดูใน Settings → Apps → TTSVoiceApp → Battery → Unrestricted

### ปัญหา: พูดภาษาไทยไม่ได้ / พูดผิดภาษา

**วิธีแก้:**
1. ติดตั้ง Google Text-to-Speech จาก Play Store
2. Settings → Language & Input → Text-to-speech output
3. เลือก Google Text-to-Speech เป็น Engine หลัก
4. กด Settings → ดาวน์โหลด Thai Language Pack
5. ทดสอบ: กด "Listen to an example"

### ปัญหา: เชื่อมต่อ API จากคอมไม่ได้

**วิธีแก้:**
1. ✅ เช็คว่าบริการรันอยู่ (สีเขียวในแอพ)
2. ✅ เช็ค IP Address ถูกต้อง
3. ✅ Android และ Computer อยู่ Wi-Fi เดียวกัน
4. ✅ ลอง curl http://<android-ip>:8765/health
5. ✅ เช็ค Firewall บนคอม

### ปัญหา: ไม่ Auto-start หลัง Reboot

**วิธีแก้:**
1. Settings → Battery → Unrestricted
2. ให้สิทธิ์ Autostart (Xiaomi, Oppo, Vivo)
3. เพิ่มในรายการ "Protected Apps" (Huawei)
4. อาจต้องเปิดแอพครั้งแรกหลัง Boot บางรุ่น

---

## 🎯 คุณสมบัติที่ได้

✅ **บริการ Background** - รันต่อเนื่องในพื้นหลัง  
✅ **REST API Server** - พอร์ต 8765  
✅ **รองรับภาษาไทย** - แปลงข้อความไทยเป็นเสียง  
✅ **รองรับภาษาอังกฤษ** - แปลงข้อความอังกฤษเป็นเสียง  
✅ **ตรวจจับภาษาอัตโนมัติ** - ไม่ต้องระบุภาษา  
✅ **Queue System** - จัดการคำขอหลายๆ รายการ  
✅ **Auto-start** - เริ่มอัตโนมัติเมื่อบูตเครื่อง  
✅ **React Native UI** - ควบคุมและทดสอบได้ง่าย  
✅ **Network Access** - เข้าถึงจากอุปกรณ์อื่นได้  

---

## 💡 เคล็ดลับการใช้งาน

### ใช้ Static IP
ตั้ง Static IP ให้ Android เพื่อ IP ไม่เปลี่ยน:
- Settings → Wi-Fi → แตะ Wi-Fi ที่เชื่อม
- Advanced → IP Settings → Static
- ใส่ IP ที่ต้องการ (เช่น 192.168.1.100)

### สร้าง Shortcut บนคอม
สร้าง `.bat` file เพื่อส่งข้อความง่ายๆ:
```bat
@echo off
curl -X POST http://192.168.1.100:8765/speak ^
  -H "Content-Type: application/json" ^
  -d "{\"text\":\"สวัสดีครับ\"}"
pause
```

### ผสานกับ Home Automation
- Home Assistant
- Node-RED  
- Python scripts
- IoT devices

---

## 📝 หมายเหตุสำคัญ

### ความปลอดภัย
⚠️ **ไม่มีการตรวจสอบสิทธิ์** - ใครก็ใช้ได้ถ้าอยู่ Wi-Fi เดียวกัน  
⚠️ **ใช้ในเครือข่ายที่เชื่อถือเท่านั้น**  
⚠️ **อย่าเปิดออก Internet สาธารณะ**  

### การใช้แบตเตอรี่
- บริการใช้แบตเตอรี่น้อยมาก
- รันแบบ Foreground Service มี Notification
- ถ้าไม่ใช้ควร Stop Service เพื่อประหยัดแบต

---

## 🎊 สำเร็จแล้ว!

APK ของคุณพร้อมใช้งานแล้ว! ทำตามขั้นตอนด้านบนเพื่อ:
1. ✅ ติดตั้งบน Android
2. ✅ ตั้งค่า Google TTS
3. ✅ เริ่มบริการ
4. ✅ ทดสอบการทำงาน
5. ✅ เชื่อมต่อจากอุปกรณ์อื่น

**มีความสุขกับการใช้งาน! 🎉**

---

## 📞 ต้องการความช่วยเหลือ?

1. **คู่มือภาษาอังกฤษ:** [README.md](../README.md)
2. **Quick Start:** [QUICKSTART.md](../QUICKSTART.md)
3. **Setup Guide:** [SETUP.md](../SETUP.md)
4. **ดู Logs:** `adb logcat | grep TTS`
